export enum RolPersonal {
  ADMIN = 'admin',
  USER = 'user',
  ENCARGADO = 'encargado',
  /** Sólo ve el presentismo (equipo, horarios, pendientes, reportes). Nada más. */
  PRESENTISMO = 'presentismo',
}
